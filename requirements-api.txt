# Runtime-only dependencies for the FINd FastAPI service.
# This is a strict subset of requirements.txt — excludes notebook,
# benchmarking, profiling, and test tooling that are not needed inside
# the production API container. Used by the Dockerfile.
#
# Full requirements.txt remains the dev/CI install (it's a superset).

fastapi
uvicorn[standard]
python-multipart

# FINd_optimized + imagehash dependencies
numpy
Pillow
imagehash
